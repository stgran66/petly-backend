const { User } = require('./user');
const { News } = require('./news');
const { Service } = require('./service');
const { Pet } = require('./pet');

module.exports = {
  User,
  News,
  Service,
  Pet,
};
